server:
  host: "localhost"
  port: 8080
  mark_processed_after_run: true

llm:
  endpoint: "http://localhost:11434"
  model: "mlx-community/Llama-3.2-3B-Instruct-4bit"
  # api_key: ""  # or set MTUI_LLM_API_KEY env var; leave unset for local models
  temperature: 0.2
  max_tokens: 2048
  top_p: 0.9

relevance:
  categories:
    - "important category"
